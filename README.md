lightweight Python automation tool for checking network host availability.
Reads target IPs/domains from targets.txt
Performs reachability checks with timeout & error handling
```bash
python3 main.py

